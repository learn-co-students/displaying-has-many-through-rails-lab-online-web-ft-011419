<h1>Appointments for <%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |app| %>
    <li><%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> : <%= link_to app.patient.name, patient_path(app.patient) %></li>
  <% end %>
</ul>
